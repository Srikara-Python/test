/* package gui;

import java.awt.Color;

import javax.swing.*;

public class MyFrame extends JFrame {
    MyFrame(){  // JFrame is a gui window to add components in iy
        this.setTitle("JFrame Title");
        this.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // Hide/do nothing on close
        this.setResizable(false);
        this.setSize(420,420); // Dimension
        this.setVisible(true); // Makes this visible
        ImageIcon logo = new ImageIcon("download.png");
        this.setIconImage(logo.getImage());
       // this.getContentPane().setBackground(Color.green);
       this.getContentPane().setBackground(new Color(154, 156, 246));
        JLabel label = new JLabel();
        label.setText("Hello World!");
        this.add(label);

    }

}


 */